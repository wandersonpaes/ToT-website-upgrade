import React from 'react';
import { Container } from './styles';

const Layout: React.FC = () => {
    return (
        <Container>
            <h1>TOT</h1>
        </Container>
    );
}

export default Layout;